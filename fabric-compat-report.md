# 🧩 Relatório de Compatibilidade com Fabric (React Native)

> Gerado automaticamente em 12/11/2025, 16:41:29

### ✅ Pacotes Compatíveis

| Pacote | Status |
|--------|--------|
| @expo/react-native-action-sheet | JS only (sem código nativo) |
| @notifee/react-native | nenhum problema detectado |
| @react-native-community/geolocation | usa codegenConfig (ok) |
| @react-native-firebase/analytics | nenhum problema detectado |
| @react-native-firebase/app | nenhum problema detectado |
| @react-native/new-app-screen | JS only (sem código nativo) |
| @react-navigation/bottom-tabs | JS only (sem código nativo) |
| @react-navigation/elements | JS only (sem código nativo) |
| @react-navigation/native | JS only (sem código nativo) |
| @react-navigation/native-stack | JS only (sem código nativo) |
| @tanstack/react-query | JS only (sem código nativo) |
| axios | JS only (sem código nativo) |
| babel-plugin-module-resolver | JS only (sem código nativo) |
| cpf-cnpj-validator | JS only (sem código nativo) |
| crypto-js | JS only (sem código nativo) |
| geolib | JS only (sem código nativo) |
| lodash | JS only (sem código nativo) |
| moment | JS only (sem código nativo) |
| moti | JS only (sem código nativo) |
| patch-package | JS only (sem código nativo) |
| postinstall-postinstall | JS only (sem código nativo) |
| react | JS only (sem código nativo) |
| react-native | usa codegenConfig (ok) |
| react-native-animatable | JS only (sem código nativo) |
| react-native-animated-dots-carousel | JS only (sem código nativo) |
| react-native-avoid-softinput | usa codegenConfig (ok) |
| react-native-blob-util | usa codegenConfig (ok) |
| react-native-camera-kit | usa codegenConfig (ok) |
| react-native-compressor | usa codegenConfig (ok) |
| react-native-device-info | nenhum problema detectado |
| react-native-geolocation-service | nenhum problema detectado |
| react-native-gesture-handler | usa codegenConfig (ok) |
| react-native-ios-context-menu | nenhum problema detectado |
| react-native-linear-gradient | nenhum problema detectado |
| react-native-masked-text | JS only (sem código nativo) |
| react-native-mmkv | usa codegenConfig (ok) |
| react-native-onesignal | nenhum problema detectado |
| react-native-permissions | usa codegenConfig (ok) |
| react-native-popup-menu | JS only (sem código nativo) |
| react-native-rate | nenhum problema detectado |
| react-native-reanimated | usa codegenConfig (ok) |
| react-native-safe-area-context | usa codegenConfig (ok) |
| react-native-screens | usa codegenConfig (ok) |
| react-native-sfsymbols | nenhum problema detectado |
| react-native-share | usa codegenConfig (ok) |
| react-native-size-matters | JS only (sem código nativo) |
| react-native-ui-devkit | JS only (sem código nativo) |
| react-native-uuid | JS only (sem código nativo) |
| react-native-vector-icons | usa codegenConfig (ok) |
| react-native-video | nenhum problema detectado |
| react-native-view-shot | usa codegenConfig (ok) |
| react-native-worklets | usa codegenConfig (ok) |
| socket.io-client | JS only (sem código nativo) |
| use-debounce | JS only (sem código nativo) |
| validator | JS only (sem código nativo) |
| validator-brasil | JS only (sem código nativo) |
| @babel/core | JS only (sem código nativo) |
| @babel/preset-env | JS only (sem código nativo) |
| @babel/runtime | JS only (sem código nativo) |
| @react-native-community/cli | JS only (sem código nativo) |
| @react-native-community/cli-platform-android | JS only (sem código nativo) |
| @react-native-community/cli-platform-ios | JS only (sem código nativo) |
| @react-native/babel-preset | JS only (sem código nativo) |
| @react-native/eslint-config | JS only (sem código nativo) |
| @react-native/metro-config | JS only (sem código nativo) |
| @react-native/typescript-config | JS only (sem código nativo) |
| @types/jest | JS only (sem código nativo) |
| @types/react | JS only (sem código nativo) |
| @types/react-test-renderer | JS only (sem código nativo) |
| eslint | JS only (sem código nativo) |
| jest | JS only (sem código nativo) |
| prettier | JS only (sem código nativo) |
| react-test-renderer | JS only (sem código nativo) |
| typescript | JS only (sem código nativo) |


### ⚠️ Pacotes Potencialmente Incompatíveis

| Pacote | Status |
|--------|--------|
| @react-native-community/netinfo | peer react-native antigo |
| @react-native-community/segmented-control | peer react-native antigo |
| react-native-date-picker | peer react-native antigo |
| react-native-fast-image | peer react-native antigo |
| react-native-haptic-feedback | peer react-native antigo |
| react-native-image-viewing | peer react-native antigo |
| react-native-iphone-screen-helper | peer react-native antigo |
| react-native-launch-arguments | peer react-native antigo |
| react-native-settings | peer react-native antigo |
| react-native-snap-carousel | peer react-native antigo |


